# 사칙연산 계산기 — 한글 주석 (수정본) 🧮
def 더하기(좌변, 우변):
    """두 정수를 더한다."""
    합계 = 좌변 + 우변
    return 합계

def 나누기(분자, 분모):
    if 분모 == 0:
        raise ValueError("0으로 나눌 수 없습니다")
    return 분자 / 분모

def 곱하기(가, 나):
    return 가 * 나
