# Diffinite TODO — 2026-03-16

> **작성일**: 2026-03-15 23:35 → **갱신**: 2026-03-16 10:34  
> **현재 상태**: dev 브랜치, **253 passed / 4 skipped** ✅  
> **최종 커밋**: `[dev 60b67f1]` — 17 files, +1001/−135

---

## 완료 항목 (12건)

### Phase 2 고도화 재구현 — 6건 ✅

| # | 항목 | 변경 파일 |
|:-:|------|----------|
| 3 | `TOKEN_RE` 중복 통합 | `fingerprint.py`, `evidence.py` |
| 1 | `ClassificationThresholds` frozen dataclass (18필드) | `models.py`, `evidence.py` |
| 2 | `IDEXThresholds` frozen dataclass (8필드) | `models.py`, `evidence.py` |
| 5 | `--no-autojunk` CLI | `differ.py`, `cli.py`, `pipeline.py`, `models.py` |
| 4 | JS 템플릿 리터럴 5-state 파서 | `parser.py`, `test_parser.py` (+3) |
| 6 | `--max-index-entries` 메모리 제한 | `deep_compare.py`, `cli.py`, `pipeline.py` |

### 문서 갱신 — 4건 ✅

| 파일 | 변경 |
|------|------|
| `CHANGELOG.md` | v0.3.0 Phase 2-4 항목 추가 |
| `doc/cli_guide.md` | `--no-autojunk`, `--max-index-entries` |
| `doc/modules.md` | 5-state, autojunk, TOKEN_RE, 2 dataclasses |
| `doc/evidence_channels.md` | dataclass, IDEXThresholds, TOKEN_RE |
| `doc/development.md` | 프로파일 추가 가이드 업데이트 |

### 코드 품질 — 2건 ✅

| # | 수정 |
|:-:|------|
| 11 | `_extract_identifiers(extension=)` — Java stopword 언어별 적용 |
| 12 | `_run_profile_scores(raw_source_a/b=)` — comment 채널 원본 분리 |

---

## 잔여 백로그 — 4건

> **권장**: 각 항목은 독립적이므로 **별도 대화**에서 1~2건씩 진행.

### #13. SOCO 코퍼스 확장

**상태**: 보류 (사용자 지시 대기)  
**목적**: 646쌍 코퍼스에 SOCO C/C++ test set 추가 → 다국어 커버리지 확대  
**의존성**: SOCO dataset 다운로드 필요  
**예상 작업량**: 2~3시간

**후속 프롬프트**:
```
report/todo_260316.md를 읽고 #13 SOCO 코퍼스 확장을 수행해줘.

작업 순서:
1. SOCO C/C++ test set을 TDD/corpus/data/soco/ 에 배치
2. conftest.py에서 SOCO 데이터 로더 추가
3. test_soco_sensitivity.py 작성 (L1-L4 수준별 검증)
4. 기존 646쌍 기준 회귀 없음 확인
5. doc/progress.md 갱신 후 dev 커밋
```

---

### #14. IDEX 임계값 ROC AUC 교정

**상태**: 스크립트 준비 (`TDD/corpus/idex_threshold_calibration.py`)  
**목적**: `IDEXThresholds` 기본값을 ROC AUC 최적화로 교정  
**의존성**: 코퍼스 데이터 (현재 646쌍으로 실행 가능)  
**예상 작업량**: 1~2시간

**후속 프롬프트**:
```
report/todo_260316.md를 읽고 #14 IDEX 임계값 ROC AUC 교정을 수행해줘.

작업 순서:
1. TDD/corpus/idex_threshold_calibration.py 실행 → 최적 임계값 도출
2. models.py의 IDEXThresholds 기본값 업데이트
3. TDD/legal_defense/ 테스트 전체 통과 확인
4. evidence_channels.md 갱신 후 dev 커밋
```

---

### #15. MinHash + LSH 근사 인덱싱

**상태**: 미착수  
**목적**: 대규모 코퍼스(10만+파일)에서 역 인덱스 대신 MinHash 기반 근사 매칭 → 메모리/시간 절감  
**의존성**: `datasketch` 라이브러리  
**예상 작업량**: 3~4시간

**후속 프롬프트**:
```
report/todo_260316.md를 읽고 #15 MinHash+LSH 근사 인덱싱을 수행해줘.

작업 순서:
1. pyproject.toml에 datasketch optional dependency 추가
2. deep_compare.py에 MinHashLSH 기반 run_deep_compare_lsh() 구현
3. --index-mode exact|minhash CLI 옵션 추가
4. 기존 exact 모드 대비 정확도/성능 비교 테스트
5. doc/modules.md, cli_guide.md 갱신 후 dev 커밋
```

---

### #16. PDG 병렬 분석 (Phase 4)

**상태**: 미착수  
**목적**: Use-Def 체인 기반 데이터 흐름 분석 → 의미적 클론 탐지력 향상  
**의존성**: `ast_normalizer.py`의 `pdg_tokenize()` 활용  
**예상 작업량**: 4~5시간 (가장 복잡)

**후속 프롬프트**:
```
report/todo_260316.md를 읽고 #16 PDG 병렬 분석을 수행해줘.

작업 순서:
1. ast_normalizer.py의 pdg_tokenize() 현재 상태 감사
2. deep_compare.py에 PDG 채널 통합 (7채널 확장)
3. evidence.py의 compute_channel_scores()에 pdg_winnowing 채널 추가
4. --tokenizer pdg 모드 E2E 테스트
5. evidence_channels.md, modules.md 갱신 후 dev 커밋
```

---

## 우선순위 권장

```
#14 (IDEX 교정)  → 스크립트 준비됨, 최소 노력 최대 효과
#13 (SOCO 확장)  → 데이터 확보 후 진행
#15 (MinHash)    → 대규모 배포 전 필요
#16 (PDG 분석)   → 연구 단계, 시간 여유 시 진행
```
